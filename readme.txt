- work in progress

- Windows operating system is required for audio playback

- SIFT was used since SIFT descriptors take much less space than HOG descriptors

- Logistic Regression was used because class probabilities can be computed, therefore, "Unknown" class can be implemented